{
	"name": "yaid-site",
	"version": "0.0.0",
	"description": "YAID website",
	"private": true,
	"license": "MIT",
	"author": "Hans van Leeuwen",
	"type": "module",
	"scripts": {
		"build": "bun build --minify --sourcemap=external --outdir=dist script.js && bun run build.ts",
		"lint": "eslint *.ts *.js && prettier -c ."
	},
	"dependencies": {
		"@picocss/pico": "^1.5.10",
		"bun": "^0.6.14",
		"markdown-it": "^13.0.1",
		"markdown-it-copy": "^1.2.0",
		"markdown-it-highlightjs": "^4.0.1",
		"sass": "^1.64.1"
	},
	"devDependencies": {
		"bun-types": "^0.6.14",
		"prettier": "^3.0.0"
	}
}
