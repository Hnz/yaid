{
	"name": "yaid-site",
	"version": "0.0.0",
	"description": "YAID website",
	"private": true,
	"license": "MIT",
	"author": "Hans van Leeuwen",
	"type": "module",
	"scripts": {
		"build": "bun run build.js && bun build script.js --outdir dist --sourcemap external --minify"
	},
	"dependencies": {
		"@picocss/pico": "^1.5.10",
		"bun": "^0.6.12",
		"handlebars": "^4.7.7",
		"remarkable": "^2.0.1"
	}
}
